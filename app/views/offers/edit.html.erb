 <h1>Update your offer</h1>

  <%= simple_form_for @offer do |f| %>
    <%= f.input :fee, input_html: { min: 0 }, label: 'Fee per week (€)'%>
    <%= f.input :address, :placeholder => "Paris"%>
    <%= f.input :start_date, label: 'Available from:', as: :string, required: false, input_html: { class: "datepicker"}%>
    <%= f.input :end_date, as: :string, label: 'end of offer:', required: false, input_html: { class: "datepicker"} %>
    <%= f.button :submit, "Update", :class => "btn btn-primary w-100" %>
  <% end %>
